import React from 'react';

import '../App.css';

function BookList(props) {
    const books = props
    console.log(books)
    
  

    return (
        <section className="BookList">
        </section>
    )
}

export default BookList;